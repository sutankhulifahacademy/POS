Phase 4 & 5: Multi-outlet reporting, realtime, owner role

## Role Hierarchy Change

- NEW: owner role — akses SEMUA outlet, bypass semua permission
- CHANGED: admin role — per-cabang, akses penuh HANYA ke outlet yang assigned
- owner@republikdimsum.id sekarang role=owner (sebelumnya admin)

## Phase 4: Multi-Outlet Reporting

### Dashboard (GET /reports/dashboard)
- Support outlet_id query parameter
- Owner: ALL OUTLETS atau filter ke outlet specific
- Non-owner: auto-filter ke outlet yang diizinkan
- Low-stock sekarang pakai outlet_stocks (per outlet) bukan products.stock (global)
- NEW: branch_comparison field — ranking outlet by revenue
- NEW: outlet_id field di response

### Reports/Shifts (GET /reports/shifts)
- Support outlet_id query parameter
- Auto-filter untuk non-owner
- outlet_id ditambahkan ke response

### Sales Monitoring (NEW: GET /reports/sales-monitor)
- Real-time sales feed dengan outlet info
- Filter by outlet_id
- Returns: invoice_no, total, payment_method, cashier, outlet_name, timestamp

### Branch Comparison (NEW: GET /reports/branch-comparison)
- Compare all outlets: revenue, transactions, avg transaction, discount, tax
- Ranking by revenue
- Auto-filter untuk non-owner

### Sales List (GET /sales)
- Support outlet_id query parameter
- Auto-filter untuk non-owner

## Phase 5: Realtime WebSocket

### Backend (NEW: backend/routes/realtime.py)
- WebSocket endpoint: /api/ws/realtime
- Auth via first message (token)
- ConnectionManager: broadcast events to connected clients
- Owner receives ALL events
- Non-owner receives only their outlet events
- Events: NEW_SALE, SHIFT_OPENED, SHIFT_CLOSED, LOW_STOCK, etc.
- Ping/pong keep-alive

### Integration
- POST /sales sekarang emit NEW_SALE event setelah sukses
- Helper functions: emit_new_sale, emit_shift_opened, etc.

### Nginx (frontend/nginx.conf)
- NEW: /api/ws/ location block dengan WebSocket upgrade headers
- Timeout 7d untuk long-lived connections

### Frontend
- NEW: OutletContext (context/OutletContext.js)
  - Global outlet selector state
  - Load outlets from /outlets/my
  - Owner: ALL OUTLETS option
  - Non-owner: only assigned outlets

- NEW: useRealtime hook (hooks/useRealtime.js)
  - WebSocket connection management
  - Auto-reconnect on disconnect
  - Ping keep-alive
  - Status: connected, reconnecting, disconnected

- UPDATED: Layout.js
  - Outlet selector bar di top (sticky)
  - Role label updated: owner, admin, manager, kasir, supervisor

- UPDATED: Dashboard.js
  - Use OutletContext for outlet filtering
  - Branch comparison table (when ALL OUTLETS selected)
  - Reload on outlet change

- UPDATED: App.js
  - OutletProvider wrapping app

### Docker
- requirements.txt: uvicorn[standard] + websockets
- Backend rebuilt with WebSocket support

## Tests

- 121 E2E tests: all pass
- WebSocket test: CONNECTED, PONG, NEW_SALE event — all pass
- Manual tests:
  - Owner: dashboard all outlets + branch comparison (3 outlets)
  - Owner: dashboard filter by outlet_id
  - Sales monitor: 5 recent transactions
  - Branch comparison: 3 outlets ranked
  - Shifts with outlet filter
  - Sales list with outlet filter

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
