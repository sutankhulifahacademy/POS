Phase 9-18: Leave Requests, Receipt Config, Loyalty, KDS, Stock Adj,
Tax, Coupons, Schedules, Payroll, Mobile Dashboard + Outlet Filtering Fix

## CRITICAL FIX: Outlet Filtering Across All Pages

### Problem
Selecting a specific outlet in the global outlet selector did not change
data in most pages — only Dashboard, AIAssistant, Expenses, AuditLogs
respected the OutletContext.

### Fix — Backend
- inventory.py: GET /inventory/movements + GET /inventory/stock now accept
  outlet_id and filter accordingly (with outlet authorization)
- attendance.py: GET /attendance now accepts outlet_id
- shifts.py: GET /shifts now accepts outlet_id
- products.py: GET /products now accepts outlet_id (returns outlet_stock)
- reports.py: Fixed SQL bind parameter bug where o_filter.replace(
  'outlet_id', 's.outlet_id') corrupted :outlet_id into :s.outlet_id

### Fix — Frontend
All pages now import useOutlet and pass outletIdForApi to API calls:
- Reports.js: All 6 tabs (Dashboard, Sales, ProfitLoss, Shifts, Stock,
  Reconciliation) sync with global outlet selector
- Inventory.js: Products + movements filtered by outlet
- Attendance.js: History filtered by outlet
- Shifts.js: Shift list filtered by outlet
- Products.js: Product list with outlet-specific stock
- POS.js: Syncs with global outlet selector

### Test Results
- Outlet filtering: 61 tests, all pass
  - Dashboard, Sales, Shifts, Attendance, Inventory, Stock, Products,
    Expenses, AuditLogs, Alerts, Reports, BranchComparison, SalesMonitor,
    AI (all 4 endpoints), Users — all verified per-outlet vs all-outlets

## Phase 9: Leave Requests (Cuti/Izin)

### Backend (NEW: routes/leave_requests.py)
- GET /leave-requests — list with outlet filter + my_only option
- POST /leave-requests — submit (cuti, sakit, izin, dinas_luar)
- PUT /leave-requests/{id}/approve — approve (manager+)
- PUT /leave-requests/{id}/reject — reject with reason
- DELETE /leave-requests/{id} — delete (owner or requester)

### Frontend (NEW: pages/LeaveRequests.js)
- Table with karyawan, jenis, tanggal, alasan, status
- Form pengajuan cuti
- Approve/reject buttons for pending requests

## Phase 10: Receipt/Invoice Customization

### Schema
- outlets: + receipt_header, receipt_footer, receipt_logo,
  receipt_show_cashier, receipt_show_shift, receipt_paper_width,
  receipt_font_size

### Backend (NEW: routes/receipt_config.py)
- GET /receipt-config/{outlet_id}
- PUT /receipt-config/{outlet_id}

### Frontend (NEW: pages/ReceiptConfig.js)
- Receipt header/footer/logo config
- Paper width (58mm/80mm), font size
- Show cashier/shift toggles
- Live receipt preview

## Phase 11: Customer Loyalty

### Schema (NEW)
- customer_memberships: customer_id, outlet_id, tier, points, total_spent
- point_transactions: customer_id, outlet_id, sale_id, points_change

### Backend (NEW: routes/loyalty.py)
- GET /loyalty/memberships — list with outlet filter
- GET /loyalty/customer/{id} — customer's memberships
- GET /loyalty/points/{id} — point history
- POST /loyalty/adjust-points — manual adjustment
- GET /loyalty/tiers — tier thresholds

### Frontend (NEW: pages/Loyalty.js)
- Tier cards (bronze/silver/gold/platinum)
- Memberships table
- Point adjustment modal

## Phase 12: Kitchen Display System (KDS)

### Schema (NEW)
- kitchen_orders: outlet_id, sale_id, items, status, priority, elapsed

### Backend (NEW: routes/kds.py)
- GET /kds/orders — queue with outlet filter
- PUT /kds/orders/{id}/status — new->preparing->ready->served
- PUT /kds/orders/{id}/priority
- GET /kds/stats — counts + avg wait time

### Frontend (NEW: pages/KitchenDisplay.js)
- 3-column board (New/Preparing/Ready)
- Color-coded elapsed time (green/yellow/red)
- Auto-refresh 10s

## Phase 13: Stock Adjustment with Reason

### Schema
- stock_movements: + outlet_id, adjustment_type, reference_no, approved_by

## Phase 14: Tax Configuration

### Schema
- outlets: + tax_enabled, tax_rate, tax_name, tax_inclusive,
  service_charge_enabled, service_charge_rate

### Integrated in receipt_config routes

## Phase 15: Discount/Coupon Management

### Schema (NEW)
- coupons: outlet_id, code, discount_type, discount_value, min_purchase,
  max_discount, start_date, end_date, usage_limit, usage_count

### Backend (NEW: routes/coupons.py)
- GET /coupons — list with outlet filter
- POST /coupons — create
- PUT /coupons/{id} — update
- DELETE /coupons/{id} — delete
- POST /coupons/validate — validate code + calculate discount

### Frontend (NEW: pages/Coupons.js)
- Coupon table with CRUD
- Validate coupon widget

## Phase 16: Employee Scheduling

### Schema (NEW)
- employee_schedules: outlet_id, user_id, day_of_week, start_time, end_time

### Backend (NEW: routes/schedules.py)
- GET /schedules — list with outlet filter
- POST /schedules — create
- PUT /schedules/{id} — update
- DELETE /schedules/{id} — delete

### Frontend (NEW: pages/Schedules.js)
- Weekly grid (employees x days)
- Click cell to add/edit shift

## Phase 17: Payroll Integration

### Schema (NEW)
- payroll_periods: outlet_id, period_name, start_date, end_date, status
- payroll_items: period_id, user_id, base_salary, overtime, attendance,
  deductions, net_pay

### Backend (NEW: routes/payroll.py)
- GET /payroll/periods — list with outlet filter
- POST /payroll/periods — create period
- POST /payroll/periods/{id}/process — auto-calculate salary
- GET /payroll/periods/{id}/items — detailed items

### Frontend (NEW: pages/Payroll.js)
- Period list with status badges
- Create + process buttons
- Payroll items table with summary

## Phase 18: Mobile Dashboard

### Frontend (NEW: pages/MobileDashboard.js)
- Large touch-friendly cards
- Bottom navigation (Dashboard/Sales/Alerts/Profile)
- Outlet selector for owners
- Uses existing /reports/dashboard endpoint

## Schema Migration
- migration_v2_3_phase10_18.sql — all new tables + columns + permissions + menus

## Test Results
- E2E regression: 121 passed, 0 failed
- Phase 7 suite: 66 passed, 0 failed
- Outlet filtering: 61 passed, 0 failed
- Total: 248 tests, all pass

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
