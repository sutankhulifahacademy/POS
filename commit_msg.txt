Fix outlet integration across all menus + Karyawan outlet assignment + comprehensive tests

## FIXES

### 1. Loyalty page blank
- API returned {tiers: [...]} object, frontend expected array
- Fixed: setTiers(tierRes.data?.tiers || [])
- Fixed adjust-points API field mismatch (customer_id, points_change)

### 2. Owner cannot return to dashboard from POS
- Root cause: Owner role had NO menu entries for /dashboard and /pos
- Fixed: Added all 26 menus to owner role in role_menus

### 3. Attendance not outlet-integrated
- All 36 attendance records had outlet_id = NULL
- Fixed: clock-in now auto-saves outlet_id from user assignment
- Backfilled existing NULL records to outlet 1

### 4. Shifts not outlet-integrated
- 1 shift had NULL outlet_id
- Fixed: open_shift auto-detects outlet from user assignment
- Backfilled NULL records

### 5. Sales authorization bug
- Manager could access any outlet's sales (no auth check)
- Fixed: list_sales now enforces outlet authorization

### 6. Tables not outlet-filtered
- GET /tables returned all tables regardless of outlet
- Fixed: Added outlet_id filter + authorization

### 7. Purchase Orders not outlet-integrated
- PO list/create did not filter by outlet
- Fixed: Added outlet_id param to list + auto-detect on create
- Frontend PurchaseOrders.js now uses useOutlet

### 8. Stock Transfers not outlet-filtered
- Fixed: list_transfers now filters by from/to outlet

## NEW FEATURES

### Karyawan outlet assignment
- Form now has outlet multi-select with primary outlet toggle
- Backend create_user/update_user save outlet_ids + primary_outlet_id
- Role select: owner option only shows if current user is owner
- Admin and Owner are now separate roles (admin = assigned outlets only)

### Test data per outlet
- 15 sales for outlet 2 (mix dine-in/takeaway)
- 10 sales for outlet 3 (mix dine-in/takeaway)
- 20 dine-in tables (10 each for outlets 2 & 3)
- 60 attendance records (outlets 2 & 3)
- 24 shift records (outlets 2 & 3)
- 16 expense records (outlets 2 & 3)
- 6 purchase orders (outlets 2 & 3)
- 190 outlet_stocks for all products in all outlets

## TESTS
- Comprehensive: 110 passed, 0 failed (27 categories)
- E2E regression: 121 passed, 0 failed
- Phase 7 authorization: 66 passed, 0 failed
- Total: 297 tests, all pass

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
