Phase 2 & 3: Multi-outlet database + authorization system

## Phase 2 — Database Changes

New tables:
- user_outlet_access (user_id, outlet_id) — multi-outlet authorization
- audit_logs — audit trail for all important actions
- leave_requests — leave/permission management
- alerts — notification/alert center

New columns:
- attendance.outlet_id — tie attendance to outlet
- purchase_orders.outlet_id — tie PO to outlet

New indexes:
- idx_attendance_outlet, idx_purchase_orders_outlet
- idx_orders_outlet, idx_tables_outlet
- idx_stock_movements_outlet, idx_payment_accounts_outlet
- All indexes for new tables

Migration: backend/sql/migration_v2_multi_outlet.sql (executed)
Schema: backend/sql/postgres_schema.sql (updated)

Seeded user_outlet_access:
- Admin → all outlets
- Other users → main outlet

## Phase 3 — Authorization System

New dependencies (backend/routes/auth.py):
- require_permission(module, action) — checks role_permissions table
- require_outlet_access(outlet_id) — checks user_outlet_access table
- get_user_outlets(user) — returns allowed outlet IDs
- filter_outlets_for_user(user, column) — returns SQL filter clause

Updated get_current_user:
- Now loads outlet_ids for non-admin users
- Admin gets empty list (= all outlets)

New endpoints:
- GET /outlets/my — returns user's accessible outlets + all_access flag
- GET /users/{id}/outlets — get user's outlet access
- PUT /users/{id}/outlets — update user's outlet access

Permission enforcement wired to all endpoints:
- products: create/update/delete
- categories: create/update/delete
- customers: create/update/delete
- suppliers: create/update/delete
- outlets: create/update/delete
- tables: create/update/delete
- inventory: update
- payment_accounts: create/update/delete
- card_brands: create
- purchase_orders: create/approve/delete
- stock_transfers: create
- business: manage
- menus: manage
- users: view/create/update
- roles: admin-only (require_role)

Fix: supervisor role added to UserRole literal

## Tests

- 121 E2E tests: all pass
- 32 multi-outlet tests: all pass
  - Login all roles
  - Outlet access in user object
  - /outlets/my endpoint
  - Outlet list filtering
  - Permission enforcement (kasir blocked, manager allowed)
  - User outlet access management

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
