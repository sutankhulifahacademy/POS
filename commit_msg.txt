Phase 8: Employee-Outlet Assignment, Audit Logs, Alerts, Expenses

## Schema Changes (migration_v2_2_phase8.sql)

### user_outlet_access (ALTER)
- Added is_primary BOOLEAN DEFAULT FALSE
- Added assigned_at TIMESTAMPTZ DEFAULT NOW()
- Auto-set first outlet as primary for existing records

### expenses (NEW TABLE)
- id, outlet_id, category, description, amount, expense_date
- payment_method, vendor, receipt_no, created_by, created_at, updated_at
- Indexes: outlet+date, category+date, date

### Permissions Added
- expenses: view/create/update/delete (owner, admin)
- expenses: view/create (manager)
- audit: view (owner, admin)
- alerts: view/manage (owner, admin), view (manager)

### Menus Added
- expenses (Pengeluaran) — id: ...b19
- audit_logs (Audit Log) — id: ...b20

## 1. Employee-Outlet Assignment

### Backend (users.py)
- GET /users now returns outlet info per user:
  - outlets[]: outlet_id, outlet_name, is_primary, assigned_at
  - primary_outlet: name of primary outlet
- GET /users/{id}/outlets returns outlet details with is_primary
- PUT /users/{id}/outlets accepts primary_outlet_id

### Frontend (Karyawan.js)
- Employee cards now show "🏪 Outlet Utama (+1 lainnya)"
- Role labels updated: owner, admin, manager, supervisor, kasir
- Role colors: owner/admin=gold, manager=green, supervisor=blue, kasir=gray

## 2. Audit Logs

### Backend (NEW: routes/audit_logs.py)
- GET /audit-logs — list with filtering (outlet, entity, action, user, date)
- GET /audit-logs/stats — by_action, by_entity, today_count
- log_action() helper — called from other routes
- Outlet-scoped: non-owner sees only their outlet logs

### Integration
- POST /sales now logs to audit_logs
- POST /expenses logs to audit_logs
- PUT/DELETE /expenses logs to audit_logs

### Frontend (NEW: pages/AuditLogs.js)
- Filterable table (entity, action)
- Color-coded actions (create=green, update=yellow, delete=red)
- Shows: time, user, role, action, entity, outlet

## 3. Alerts + Notifications

### Backend (NEW: routes/alerts.py)
- GET /alerts — list with filtering (is_read, category, severity, outlet)
- PUT /alerts/{id}/read — mark single alert as read
- PUT /alerts/read-all — mark all as read
- DELETE /alerts/{id} — delete (manage permission)
- create_alert() helper — called from other routes

### Auto-Generated Alerts
- Low stock after sale → inventory/critical or inventory/warning
- Large expense (>=1,000,000) → expense/warning

### Frontend (Layout.js)
- NotificationBell component in top bar
- Shows unread count badge
- Dropdown with alert list
- Auto-refresh every 30 seconds
- Mark all as read button
- Severity color indicators

## 4. Expense Tracking

### Backend (NEW: routes/expenses.py)
- GET /expenses — list with filtering (outlet, category, date range)
- GET /expenses/summary — by_category, total, by_outlet
- POST /expenses — create with outlet authorization
- PUT /expenses/{id} — update with ownership check
- DELETE /expenses/{id} — delete with ownership check
- Categories: rent, utilities, salary, supplies, maintenance, marketing, other
- Auto-alert for expenses >= 1,000,000

### Frontend (NEW: pages/Expenses.js)
- Summary cards (total, count, top categories)
- Add expense form with outlet selector
- Table with date, outlet, category, description, amount
- Delete button per row
- Outlet-aware filtering

## Test Results
- E2E regression: 121 passed, 0 failed
- Phase 7 suite: 66 passed, 0 failed
- Phase 8 manual: all endpoints 200
- Total: 187+ tests, all pass

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
