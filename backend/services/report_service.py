"""Report service — placeholder.

Report logic currently lives entirely in the route handler(s) inside
routes/reports.py. In the future, the query/aggregation logic could be
extracted into functions here so the route layer stays thin.
"""
